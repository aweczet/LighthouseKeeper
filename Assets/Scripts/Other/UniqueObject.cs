using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//jeszcze nie rozgryzlem jak to zrobic uzywajac tej klasy wiec wszystko jest na razie w skrypcie Player
public class UniqueObject : MonoBehaviour
{
    public string objectName = "default";
    public bool uniqueObjectClicked = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
